# Game projects (implementation)

**This public wiki does not host game source code.** Design, research, and agent harness notes live in `wiki/`. Runnable projects live in **separate git repos** on the operator laptop (see `ROADMAP.md` decision **D3** — private until a vertical-slice demo).

| Project | Status | Canonical path |
|---------|--------|----------------|
| **Castle sim (Stronghold 2–inspired)** | Scaffolded — implementation phase (Fork B: Godot 3D) | `/Users/claudiobarone/Projects/castle-sim/` |

When a project repo exists, add a one-line pointer here and a `wiki/entities/projects/<slug>.md` page. Never commit prod secrets, API keys, or unreleased asset binaries to the public wiki.
